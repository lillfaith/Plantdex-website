node_modules/
.next/
out/
build/
.DS_Store
*.pem

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

.env
.env*.local

next-env.d.ts
*.tsbuildinfo

.vercel
coverage/

# Deck print masters are large binaries and are not kept in the web repo.
# scripts/build_deck.py reads them from a local path passed on the command line.
deck-source/

# Python bytecode from the sprite/deck build scripts.
__pycache__/
*.pyc

# A generated review sheet: every creature at every stage, sprite sheets embedded as
# data URIs so it opens with no server. Rebuild it rather than committing 700K of
# base64 that goes stale the moment a sprite is redrawn.
gallery.html
